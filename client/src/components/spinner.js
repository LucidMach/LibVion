import "./spinner.css";

const Loading = () => {
  return (
    <div style={{ display: "flex", justifyContent: "center" }}>
      <div className="Loading"></div>
    </div>
  );
};

export default Loading;
